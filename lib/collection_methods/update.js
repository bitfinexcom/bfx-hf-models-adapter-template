'use strict'

/**
 * Must update one or multiple documents in-place, based on the specified
 * criteria. For criteria format, see find()
 *
 * @param {Object} db
 * @param {Object} model
 * @param {Array[]} criteria
 * @param {Object} data
 * @return {Promise} p - resolves to updated object
 */
module.exports = async (db, model, criteria = [], data) => {
  return {}
}
