'use strict'

/**
 * Must return all documents in the collection
 *
 * @param {Object} db
 * @param {Object} model
 * @return {Promise} p - resolves to array
 */
module.exports = async (db, model) => {
  return []
}
