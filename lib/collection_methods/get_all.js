'use strict'

/**
 * Returns all records in a collection
 *
 * @memberof COLLECTION_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - model instance
 * @returns {Promise} p - resolves to an array with all records
 */
const getAll = async (db, model) => []

module.exports = getAll
