'use strict'

/**
 * Must remove all documents from the collection
 *
 * @param {Object} db
 * @param {Object} model
 * @return {Promise} p - resolves with number of removed documents
 */
module.exports = async (db, model) => {
  return 0
}
