'use strict'

/**
 * Inserts all the provided documents to the path in the provided model
 *
 * @memberof COLLECTION_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - model instance
 * @param {object[]} docs - documents
 * @returns {Promise} p - resolves to array
 */
const bulkInsert = async (db, model, docs) => []

module.exports = bulkInsert
