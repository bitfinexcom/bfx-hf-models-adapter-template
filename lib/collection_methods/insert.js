'use strict'

/**
 * Must insert the document into the DB, and return the inserted value
 *
 * @param {Object} db
 * @param {Object} model
 * @param {Object} document
 * @return {Promise} p - resolves to inserted document
 */
module.exports = async (db, model, doc) => {
  return {}
}
