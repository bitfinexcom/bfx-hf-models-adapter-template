'use strict'

/**
 * Removes all documents on a model collection
 *
 * @memberof MAP_METHODS
 *
 * @param {object} db - db instance
 * @param {object} model - model instance
 * @returns {Promise} p - resolves to number of removed records
 */
const rmAll = async (db, model) => 0

module.exports = rmAll
