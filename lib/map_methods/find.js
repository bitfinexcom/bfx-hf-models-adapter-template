'use strict'

/**
 * Queries a document based on the provided criteria
 *
 * @param {Object} db
 * @param {Object} model
 * @param {Object} criteria
 * @return {Promise} p - resolves to array of filtered documents
 */
module.exports = async (db, model, criteria) => {
  return []
}
