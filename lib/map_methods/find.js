'use strict'

/**
 * Queries a document based on the provided criteria
 *
 * @memberof MAP_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - model instance
 * @param {bfx-hf-models.DBAdapterCriteria[]} criteria - array of criteria to
 *   be met for a record to be updated
 * @returns {Promise} p - resolves to array of filtered documents
 */
const find = async (db, model, criteria) => []

module.exports = find
