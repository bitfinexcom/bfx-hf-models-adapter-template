'use strict'

/**
 * Must create a new document on the map
 *
 * @param {Object} db
 * @param {Object} model
 * @param {string} key
 * @param {Object} data
 * @return {Promise} p - resolves to created document
 */
module.exports = async (db, model, key, data) => {
  return {}
}
