'use strict'

/**
 * Fetch a record by ID
 *
 * @memberof MAP_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - initialized model instance
 * @param {object|string|number} docOrID - record to fetch or its ID
 * @returns {Promise} p - resolves to matching record
 */
const get = async (db, model, docOrID) => ({})

module.exports = get
