'use strict'

/**
 * Must create a document by ID or partial document for key generation
 *
 * @param {Object} db
 * @param {Object} model
 * @param {Object} doc - document to create
 * @return {Promise} p - resolves to created document
 */
module.exports = async (db, model, doc) => {
  return {}
}
