'use strict'

/**
 * Queries all documents on a model collection
 *
 * @memberof MAP_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - model instance
 * @returns {Promise} p - resolves to array of all documents
 */
const getAll = async (db, model) => []

module.exports = getAll
