'use strict'

/**
 * Queries all documents on a model collection
 *
 * @param {Object} db
 * @param {Object} model
 * @return {Promise} p - resolves to array of all documents
 */
module.exports = async (db, model) => {
  return []
}
