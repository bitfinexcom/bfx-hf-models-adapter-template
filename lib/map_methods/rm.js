'use strict'

/**
 * Must removed a document by ID or partial document for key generation
 *
 * @param {Object} db
 * @param {Object} model
 * @return {Promise} p - resolves to removed document
 */
module.exports = async (db, model) => {
  return {}
}
