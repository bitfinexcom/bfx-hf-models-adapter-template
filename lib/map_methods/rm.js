'use strict'

/**
 * Removes a record by ID
 *
 * @memberof MAP_METHODS
 *
 * @param {object} db - db instance
 * @param {bfx-hf-models.Model} model - initialized model instance
 * @param {object|string|number} docOrID - record to remove or its ID
 * @returns {Promise} p - resolves to removed record
 */
const rm = async (db, model, docOrID) => ({})

module.exports = rm
